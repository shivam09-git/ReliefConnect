import Donation from '../models/donationModel.js'; export const createDonation = async (req,res)=>{ try{ const d = await Donation.create(req.body); res.status(201).json(d); }catch(e){ res.status(500).json({message:e.message}); } }; export const getDonations = async (req,res)=>{ try{ const list = await Donation.find().sort({createdAt:-1}); res.json(list); }catch(e){ res.status(500).json({message:e.message}); } };