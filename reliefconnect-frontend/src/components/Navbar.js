import React from 'react'; import { Link, useNavigate } from 'react-router-dom'; import { clearToken, getToken } from '../utils/auth'; export default function Navbar(){ const nav = useNavigate(); const token = getToken(); function logout(){ clearToken(); nav('/'); } return (<header className='container navbar'><div className='logo'><div className='badge'>RC</div><div>ReliefConnect<br/><small style={{color:'#6b7280',fontWeight:500}}>Connect help with those in need</small></div></div><nav className='navlinks'><Link to='/'>Home</Link><Link to='/dashboard'>Dashboard</Link><Link to='/request'>Request Help</Link>{token ? <button onClick={logout} className='login-btn' style={{border:'none'}}>Logout</button> : <><Link to='/login'>Login</Link><Link to='/register'>Register</Link></>} </nav></header>); }